"""
CellMap Training Pipeline.

Fresh zarr-based training using cellmap-data for data loading,
custom partial annotation losses, and MONAI/CSC model architectures.

Replaces the old NIfTI-based pipeline (archived in archive/).
"""
